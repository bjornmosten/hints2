[package]
name = "mouse_service"
version = "0.1.0"
edition = "2021"

[dependencies]
hints = { path = "../hints" }
backends = { path = "../backends" }
utils = { path = "../utils" }
serde = { version = "1.0", features = ["derive"] }

[[bin]]
name = "hintsd"
path = "src/main.rs"
